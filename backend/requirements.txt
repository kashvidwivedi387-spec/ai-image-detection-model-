fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
Pillow>=10.0
numpy>=1.26
opencv-python-headless>=4.9

# Optional — only needed if you swap pattern_score() for the real
# Hugging Face model described at the bottom of detector.py
# transformers>=4.40
# torch>=2.2
